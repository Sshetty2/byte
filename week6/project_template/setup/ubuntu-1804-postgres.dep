#!/usr/bin/env bash

apt-get -y update

apt-get -y install postgresql postgresql-contrib python3-pip

pip3 install virtualenv

virtualenv -p python3 --no-site-packages run/lib

source run/lib/bin/activate

pip3 install -r setup/requirements.txt